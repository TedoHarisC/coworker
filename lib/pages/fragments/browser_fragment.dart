import 'package:flutter/material.dart';

class BrowserFragment extends StatelessWidget {
  const BrowserFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
